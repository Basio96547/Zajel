# SecureMessenger — خريطة المشروع

Kotlin، مشروع Gradle متعدد الوحدات؛ رسول آمن يجمع Signal Protocol + تشفير ما بعد الكم (ML-KEM-768) + خصوصية بيانات وصفية (mailbox ids مشتقة من سر ثنائي عبر QR، تتجدد دورياً، لا تكشف الهوية لخادم الترحيل).

## لا تقرأ هذه الملفات كاملة أبداً (مستبعدة أصلاً عبر .claude/settings.json)
`.gradle/**`، `.kotlin/**`، `.idea/**`، `build/**` وما يعادلها داخل كل وحدة (`app/build`, `core/build`, `desktop/build`, `hisn/build`, `relay/build`).

## أول خطوة قبل أي استكشاف: PROJECT-INDEX.md
⚠ فهرس **نصّي (regex + تتبّع عمق الأقواس)، وليس تحليل AST حقيقياً** — الوصول لمترجم Kotlin برمجياً يتطلب JVM + classpath ثقيل غير متناسب. دقيق غالباً (يعطي التوقيع والـ KDoc والاستخدام العكسي) لكن قد يُخطئ في حالات معقّدة جداً. يفهرس مستوى الملف والمستوى الأول داخل class/object فقط.
يُعاد توليده تلقائياً عند إيقاف/مسح/ضغط الجلسة (Stop hook)، أو يدوياً بـ `node scripts/gen-index-kotlin.mjs`.

## بنية الوحدات (Gradle modules)
| الوحدة | الدور المرجّح |
|---|---|
| `core/` | منطق التشفير والبروتوكول المشترك (`crypto/`: PqKem, MailboxToken, LibsodiumWrapper, ChatPayloads...) — الوحدة الأكثر توثيقاً بـ KDoc |
| `app/` | تطبيق Android (اختبارات `androidTest`+`test` تغطي التشفير أيضاً) |
| `desktop/` | عميل سطح المكتب |
| `relay/` | خادم الترحيل (الـ mailbox العمياء) |
| `hisn/` | وحدة إضافية (غرضها الدقيق غير مؤكَّد بفحص كود عميق — تحقّق باسمها الفعلي عند الحاجة) |

## قواعد البحث (لتقليل التوكن)
1. ابدأ بـ Grep على `PROJECT-INDEX.md`، ثم Grep بوضع `files_with_matches` على الكود إن لزم.
2. لا تقرأ ملفاً يتجاوز 300 سطر كاملاً؛ استخدم `offset`/`limit` بعد تحديد رقم السطر.
3. أي بحث يمسّ أكثر من 5 ملفات أو بلا تخمين مباشر → فوّضه لوكيل فرعي (subagent_type: Explore) بدل تكراره يدوياً.
4. استخدم `/clear` بين مهام غير مترابطة.

## ملاحظة
هذا مشروع تشفير حسّاس (بروتوكولات أمان حقيقية، post-quantum crypto) — أي تعديل على `core/src/main/kotlin/.../crypto/` يستحق حذراً إضافياً وفهماً كاملاً للتوثيق المرافق (KDoc طويل ومفصّل عمداً في هذه الوحدة) قبل التغيير، وليس مجرد تعديل سطحي.
