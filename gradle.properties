android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.java.home=C:\\Program Files\\Android\\Android Studio\\jbr
kotlin.daemon.jvmargs=-Xmx2g
kotlin.compiler.execution.strategy=in-process
android.bundle.enableUncompressedNativeLibs=true
# Blind mailbox relay (see relay/). Blank this line out to compile a build with
# no relay path at all — local network only, nothing ever leaves the LAN.
relayUrl=https://sm-blind-mailbox.basil0552106933.workers.dev
# Username directory (see directory/) — separate opt-in service, not yet
# deployed. Left blank on purpose: compiles the app with username
# search/claim fully absent until directory/ is actually deployed (wrangler
# d1 create, then wrangler deploy) and this is set to that Worker's URL.
directoryUrl=
