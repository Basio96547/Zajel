# SecureMessenger — Security Audit Checklist

**النسخة:** 3.0 — أُعيدت كتابتها بالكامل بعد التحقّق البندي من كل ادّعاء مقابل الكود الفعلي والاختبار على جهاز حقيقي.
**آخر تحقّق فعلي:** `connectedDebugAndroidTest` — 27/27 نجاح (app) + 24/24 نجاح (hisn) على Samsung Galaxy S25 Ultra، Android 16 / API 36 — 2026-07-09.
**منهجية هذا الملف:** كل بند مُعلَّم ✅ تحقّقتُ منه بقراءة الكود المصدري أو بتشغيل فعلي — لا افتراضات ولا نسخ من قالب عام. أي بند غير مؤكَّد يُعلَّم صراحةً بدل تخمين حالته. الإصدار السابق (2.0) احتوى عدّة ادّعاءات لم تصمد أمام هذا التحقّق (انظر قسم "تصحيحات جوهرية" أسفله) — إن رأيت مرجعاً لها في مكان آخر فهو خطأ.

---

## 1. Cryptographic Security

| البند | الحالة | التوضيح |
|---|---|---|
| التشفير يستخدم libsodium (NaCl) | ✅ | `crypto_box`, `crypto_secretbox`, `crypto_generichash` (blake2b)، عبر `LibsodiumWrapper.kt` |
| Signal Protocol (Double Ratchet) | ✅ | الصنف الفعلي هو `SignalProtocol` (`crypto/SignalProtocol.kt`) — X3DH + راتشيت DH + راتشيت متماثل لكل رسالة |
| Perfect Forward Secrecy | ✅ | `deriveMessageKey`/`advanceChainKey` مفصولا النطاق (keyed blake2b بثوابت مختلفة 0x01/0x02) — أُصلح 2026-07-09 بعد اكتشاف أنّهما كانا يُنتجان قيمة مشتقّة من بعضهما |
| تخزين المفاتيح في Android Keystore | ✅ | `AndroidKeyStoreManager.kt`، `setUserAuthenticationRequired(false)` صراحةً (قرار منتجي مقصود — راجع §4) |
| مسح مفاتيح الرسائل من الذاكرة | ✅ | `LibsodiumWrapper.secureWipe()` بعد كل فكّ/تشفير في `SignalProtocol.decryptMessageInternal` |
| مفتاح قاعدة البيانات لا يُترك في String | ✅ | `SecureDatabase.kt` يُرمِّز الـ`CharArray` مباشرة إلى بايتات دون `String` وسيط؛ `close()` يمحو الـ`CharArray` بـ`0.toChar()` |
| لا مفاتيح مُضمَّنة في الكود | ✅ | كل المفاتيح تُولَّد ديناميكياً (`generateKeyPair`، إلخ) |
| توليد الأرقام العشوائية | ✅ | `SecureRandom` (Kotlin/Java) + `libsodium.randombytes_buf` (عبر lazysodium) |

**تصحيح على مسوَّدة سابقة:** عبارة مرور قاعدة البيانات (`DevicePassphrase.kt`) **لا** تُشتقّ عبر HKDF — هي ٣٢ بايت عشوائية من `SecureRandom` مباشرة، تُخزَّن في `EncryptedSharedPreferences`. لا حاجة لـ HKDF هنا (المصدر عشوائي بالفعل)، لكن ذُكر خطأً في مسوَّدة سابقة أنّها "مُشتقّة عبر HKDF".

---

## 2. Network Security

| البند | الحالة | التوضيح |
|---|---|---|
| لا خادم Backend مركزي | ✅ | P2P محلي بالكامل — لا Cloudflare، لا D1 (كانا موجودين في بنية سابقة، أُزيلا بالكامل) |
| اكتشاف الأقران | ✅ | mDNS/NSD مع رمز مُدوَّر كل ساعة (`SecureMessagingClient.kt` — `TOKEN_ROTATION_MS`) |
| E2EE على مستوى التطبيق | ✅ | الحمولة مشفّرة بالكامل (راتشيت + `sealTo`) قبل مغادرة الجهاز |
| تثبيت TOFU | ✅ | `handleBundleAnnounce` يرفض أي `identityKey` لا يطابق المفتاح المُثبَّت عند الاقتران |
| سقف الاتصالات المتزامنة | ✅ | ٢٠ اتصالاً (`LocalRelayServer.kt` — `MAX_CONCURRENT_CONNECTIONS`) |
| سقف حجم الإطار | ✅ | ١٢ ميغابايت (`LocalRelayServer.kt` — `MAX_FRAME_CHARS`)، أُضيف 2026-07-09 |
| توثيق `ack` ضد التزوير | ✅ | `AckToken.compute()`: `blake2b(envelopeId, key=HKDF(مفتاح-الهوية, "ack-mac-v1"))`، مختوم داخل الظرف، يتحقّق `handleAck` منه بزمن ثابت — أُضيف 2026-07-09 |
| استنزاف مفاتيح OTK | ✅ مُخفَّف | خنق ١٠ ثوانٍ بين تسليم الحزمة لنفس `userId` المُدَّعى |

### ⚠️ نص صريح (Cleartext) — **متعمَّد، وليس مُعطَّلاً**

**تصحيح جوهري على مسوَّدة سابقة ادّعت `android:usesCleartextTraffic="false"` — هذا خطأ وقائع.** القيمة الفعلية في `AndroidManifest.xml:43` هي **`"true"`**، عن قصد: كل اتصال هو مقبس `ws://` مباشر بين جهازين على نفس شبكة الواي-فاي، والحمولة مشفّرة بالكامل على مستوى التطبيق قبل أن تلمس المقبس — لذا "النص الصريح" هنا يعني نقل بايتات مشفّرة أصلاً، لا بيانات حسّاسة خام. اختبار `testCleartextTrafficIntentionallyAllowedForLocalP2P` (أُعيد تسميته وتصحيحه 2026-07-09) يؤكّد هذا صراحةً بدل الادّعاء العكسي.

**محدودية حقيقية لا تزال مفتوحة (لم تُصلَح، على عكس ما أوحت مسوَّدة سابقة):** حقول `userId` ومفتاح الهوية العلني تمرّ **نصّاً صريحاً** ضمن إطارَي `challenge`/`bundle_announce` (`SecureMessagingClient.kt:746,868,870`) — متنصّت سلبي على الشبكة يستطيع ربط الجهاز عبر الجلسات رغم تدوير رمز الاكتشاف، لأنّ هذين الحقلين يكشفان هوية ثابتة. هذه ثغرة **متوسطة الخطورة** من الجولة الأولى للتدقيق (2026-07-09) لم تُعالَج — أُجِّلت بوعي (تكلفة إعادة هيكلة عالية مقابل خطورة عملية محدودة لأنّ المهاجم يحتاج وجوداً فعلياً على نفس شبكة الواي-فاي). **هذا لا علاقة له بعزل الوسائط في `docs/adr/0001` — ذاك عن فكّ الوسائط، لا عن سرّية البروتوكول؛ خلطهما في مسوَّدة سابقة كان خطأً مفاهيمياً.** الإصلاح الحقيقي (إن أُريد لاحقاً) هو تغليف المقبس المحلي بـTLS ذاتي التوقيع مُثبَّت على مفتاح هوية النِّد، أو نقل هذين الحقلين داخل الطبقة المختومة.

**بقايا أُزيلت 2026-07-09:** `testCertificatePinning` كان يبني `CertificatePinner` بمفاتيح وهمية (`sha256/AAAA...`) لنطاق `"example.com"` غير موجود في التطبيق، وبقية اختبار `testTlsVersionEnforced` — كلاهما بقايا من بنية Cloudflare/D1 القديمة، لا يتحقّقان من أي شيء حقيقي (لا `certificatePinner` مربوط بـ`OkHttpClient` الفعلي أصلاً). حُذفا من `SecurityConfigurationTests.kt`.

---

## 3. Data Storage Security

| البند | الحالة | التوضيح |
|---|---|---|
| قاعدة البيانات مشفّرة | ✅ | SQLCipher (`net.zetetic:sqlcipher-android`)، AES-256 |
| `secure_delete` مفعَّل على كل اتصال | ✅ | نُقل من `onCreate` إلى `onOpen` 2026-07-09 (كان يُطبَّق مرّة واحدة فقط سابقاً) |
| لا بيانات حسّاسة نصية في SharedPreferences | ✅ | `EncryptedSharedPreferences` لكل شيء عبر `SecurePreferences.kt` |
| رموز الوصول/الطوارئ | ✅ | بصمة مُملَّحة + تحقّق بزمن ثابت، لا افتراضي مُضمَّن (كان "1997" — أُزيل 2026-07-09) |
| النسخ الاحتياطي مُعطَّل | ✅ | `allowBackup="false"` + `fullBackupContent="false"` |
| قواعد استخراج البيانات | ✅ | صُحِّح مخطط `data_extraction_rules.xml` إلى `<data-extraction-rules>` (API 31+) 2026-07-09 — كان بمخطط `full-backup-content` القديم المُتجاهَل فعلياً |
| ملفات الوسائط تُحذف مع الرسالة | ✅ | `deleteMediaBlobFor()` في كل مسارات الحذف — كانت تبقى للأبد سابقاً |
| مسح شامل (`wipeAllData`) | ✅ | `NonCancellable` + يمسح القاعدة والوسائط والكاش وكل `SecurePreferences` — كان يمسح القاعدة فقط سابقاً |

---

## 4. Application Security

| البند | الحالة | التوضيح |
|---|---|---|
| `FLAG_SECURE` | ✅ | `MainActivity.kt:34` |
| بوابة البصمة مربوطة تشفيرياً | ✅ | `DisguiseGateKey` — مفتاح Keystore يتطلّب `CryptoObject` فعلياً، لا مجرّد رد نداء منطقي (كان الوضع سابقاً) |
| `android:debuggable="false"` في الإصدار | ✅ | `build.gradle.kts` — `release { isDebuggable = false }` |
| تصغير/تعتيم (R8) | ✅ | `isMinifyEnabled = true` في `release` |
| الصلاحيات "الخطرة" المطلوبة مبرَّرة | ✅ | `CAMERA` (مسح QR للتحقّق من المفاتيح عبر مكتبة zxing — `ScanContract()` في `NewChatScreen.kt`/`KeyVerificationScreen.kt`، لا صنف مخصَّص) و`RECORD_AUDIO` (رسائل صوتية) — كلاهما ميزة أمنية/وظيفية أصيلة، لا صلاحية زائدة |
| كشف الروت / Play Integrity | 🚫 غير مُطبَّق، عن قصد | مرفوض بوعي: يتطلّب Google Play Services (اتصال دائم بخوادم Google يناقض "لا مراقب مركزي")، يعاقب مستخدمي GrapheneOS/LineageOS، ويتجاوزه خصم من فئة Pegasus بسهولة |

**ملاحظة صريحة:** المفتاح الرئيسي (`AndroidKeyStoreManager`) غير مربوط بمصادقة المستخدم (`setUserAuthenticationRequired(false)`) للحفاظ على ميزة "الفتح الصامت بلا كلمة مرور". هذا قرار منتجي واعٍ لم يُنفَّذ حلّه (فصل بيانات حسّاسة عن عادية + مفتاح جلسة مؤقت بعد أول مصادقة) — يحتاج تصميم تجربة مستخدم قبل التنفيذ.

---

## 5. Authentication & Duress

| البند | الحالة | التوضيح |
|---|---|---|
| بوابة بصمة/قفل الجهاز قبل كشف المراسل | ✅ | `BiometricGate` مربوط بـ`DisguiseGateKey`؛ يفشل بأمان (لا كشف) إن تعذّر إنشاء المفتاح |
| رمز إكراه (duress) يمسح البيانات بصمت | ✅ | مُطبَّق فعلاً — `CalculatorScreen.onDuress` → `wipeAllData()` على `applicationScope` (`NonCancellable`)، **بعكس ما ذكرته مسوَّدة سابقة بأنّه "غير مُطبَّق"** |
| خنق تخمين الرمز | ✅ | ٥ محاولات فاشلة متتالية (بلا عامل تشغيل) → تجميد ٣٠ ثانية غير مرئي |
| مهلة جلسة / قفل بعد محاولات | 🟨 غير مُطبَّق | البصمة تُطلَب عند كل كشف للتمويه (لا "جلسة" منفصلة تنتهي بمهلة) — التصميم الحالي لا يحتاجها لأنّ كل كشف يمرّ بالبوابة أصلاً |

---

## 6. Message Security

| البند | الحالة | التوضيح |
|---|---|---|
| E2EE من طرف إلى طرف | ✅ | راتشيت مزدوج + PQ hybrid اختياري (ML-KEM) |
| رفض تخفيض PQ الصامت | ✅ | يرفض المتابعة إن أعلن الطرف مفتاح ML-KEM وفشل التغليف — أُضيف 2026-07-09 |
| رسائل مُتلاشية | ✅ | TTL اختياري، خيار المستخدم (ليس افتراضياً — قرار منتجي مفتوح) |
| حذف الرسائل المنتهية بأمان | ✅ | `deleteExpiredMessages()` + حذف ملفات الوسائط المرتبطة |
| إيصالات القراءة موثوقة | ✅ | مقصورة على جهة اتصال معروفة، ومُقيَّدة بالمستلِم فعلياً في استعلام DB (`markSentMessagesReadByClientIdsForRecipient`) — كانت تقبل أي مُرسِل سابقاً |
| إفساد حالة الراتشيت عن بُعد | ✅ مُصلَح | `decryptMessage` يأخذ لقطة ويتراجع عند أي فشل تحقّق — إطار مزوَّر واحد كان يُفسد الجلسة دائماً سابقاً |

---

## 7. Key Management

| البند | الحالة | التوضيح |
|---|---|---|
| مفاتيح الهوية مُولَّدة بأمان | ✅ | `crypto_box_keypair` عند إنشاء الملف الشخصي |
| مفاتيح لمرة واحدة (OTK) | ✅ | تُحرَق فور التسليم؛ مُخنَّقة ضد الاستنزاف (§2) |
| التحقّق من المفتاح عبر QR | ✅ | `KeyVerificationScreen.kt` + `NewChatScreen.kt`، عبر `ScanContract()` (مكتبة zxing-android-embedded) |
| إجراء استعادة مفتاح مخترَق (re-keying) | ❌ غير مُصمَّم | لا آلية حالياً لإبطال/تجديد مفتاح هوية بعد اشتباه اختراق دون حذف جهة الاتصال وإعادة الاقتران يدوياً بالكامل |

---

## 8. Code Quality & Input Validation

| البند | الحالة | التوضيح |
|---|---|---|
| تعقيم اسم الملف الوارد | ✅ | `MediaCodec.sanitizeFileName()` — قائمة سماح + رفض صريح لـ`.`/`..` (أُصلحت ثغرة اجتياز مسار حقيقية 2026-07-09) |
| اشتقاق MIME من الامتداد لا من ادّعاء الطرف الآخر | ✅ | `openInExternalApp` يستخدم `MimeTypeMap` على الاسم المُعقَّم، يتجاهل `mimeType` الوارد |
| حدّ حجم الوسائط على الاستقبال | ✅ | كان على الإرسال فقط؛ أُضيف في `saveIncomingMediaMessage` 2026-07-09 |
| فكّ صور محدود (`inSampleSize`) في كل المسارات | ✅ | وُحِّد عبر `decodeSampledBitmap`؛ `FullScreenImageViewer` كان يستخدم فكّاً غير محدود (قنبلة ضغط) — أُصلح 2026-07-09 |
| فيديو "اضغط للتحميل" | ✅ | يقطع استدعاء `MediaMetadataRetriever` التلقائي (مسار Stagefright) عن الظهور التلقائي للفقاعة |
| لا حقن SQL | ✅ | استعلامات Room مُعلَّمة بالكامل، لا `@RawQuery` ولا دمج نصّي |
| تحليل ساكن (Lint) | 🟨 مُهيَّأ جزئياً | `lint { abortOnError = false; checkReleaseBuilds = false }` — **لا يُوقف البناء عند خطأ، ولا يعمل على بناء الإصدار** — موجود لكن غير مُفعَّل كبوّابة حقيقية |
| SpotBugs | ❌ غير مُهيَّأ | لا وجود لإضافة SpotBugs في `build.gradle.kts` — ذُكر خطأً في مسوَّدة سابقة كـ"✅ نجح" |

---

## 9. Testing

| البند | الحالة | التوضيح |
|---|---|---|
| مجموعة androidTest للتشفير/الراتشيت | ✅ | `SignalProtocolTest`, `RatchetRoundtripTest`, `PqKemTest`, `MetadataPrivacyTest` |
| اختبار توثيق `ack` | ✅ | `AckAuthenticationTest` — تسليم صحيح، تزوير، توافق عكسي (بلا رمز)، معرّف ظرف مختلف، خصائص الاشتقاق |
| **تشغيل فعلي على جهاز حقيقي** | ✅ | `connectedDebugAndroidTest`: **27/27** (app) + **24/24** (hisn) — Samsung Galaxy S25 Ultra، Android 16، 2026-07-09 |
| اختبار اختراق خارجي (Pentest) | ❌ لم يُجرَ | يُنصح به قبل أي إصدار عام |
| مقاومة تحليل الذاكرة | 🟨 جزئي | مفاتيح الرسائل تُمحى (`secureWipe`)؛ النص الصريح المعروض في الواجهة يبقى في كائنات `String` (غير قابلة للمحو في JVM) — لم يُعالَج، تكلفة إعادة هيكلة كبيرة مقابل فائدة محدودة أمام خصم بصلاحيات جذر يقرأ الذاكرة مباشرة أصلاً |

---

## 10. Deployment — **غير مُتحقَّق، لا تفترض أنّه جاهز**

| البند | الحالة | التوضيح |
|---|---|---|
| توقيع الإصدار بمفتاح آمن | ❓ غير موثَّق | **لا يوجد أي `signingConfigs` في `build.gradle.kts`** — بناء `release` الحالي بلا تهيئة توقيع مخصَّصة |
| التحكّم بالإصدارات | ❓ **لا يوجد مستودع Git أصلاً** | لا `.git`، لا `.gitignore` — "استثناء الملفات الحسّاسة من git" غير قابل للتحقّق لأنّ لا نظام تحكّم إصدارات مُهيَّأ بعد |
| CI/CD | ❓ غير موجود | لا `.github/workflows` ولا أي إعداد CI في المشروع |

**تصحيح على مسوَّدة سابقة:** كل بنود هذا القسم كانت معلَّمة "✅" دون أي دليل — لم أجد ما يدعمها. عُلِّمت هنا بصدق كـ"غير موثَّق" بدل الادّعاء الكاذب بالإنجاز.

---

## محدوديات معروفة ومقبولة

| المحدودية | الخطر | التخفيف الفعلي |
|---|---|---|
| `userId`/مفتاح الهوية نصّاً صريحاً في مصافحة `challenge`/`bundle_announce` | متوسط | يتطلّب وجوداً فعلياً على نفس شبكة الواي-فاي؛ مؤجَّل بوعي (§2) |
| وصول فيزيائي للجهاز مفتوحاً | عالٍ | `FLAG_SECURE` + بوابة بصمة مربوطة تشفيرياً + رمز إكراه فعلي |
| جهاز مروَّت + برمجية تجسّس من فئة Pegasus | عالٍ جداً | لا يمكن لأي تطبيق منعه على مستوى النظام. التخفيف الفعلي: فيديو اضغط-للتحميل، حدّ حجم/تعقيم الوسائط الواردة، `decodeSampledBitmap` موحَّد؛ عزل فكّ الوسائط في عملية منفصلة **مصمَّم لا مُنفَّذ** (`docs/adr/0001-media-decode-isolated-process.md`) — شرط التنفيذ: توفّر جهاز حقيقي/محاكٍ للتحقّق من سلوك IPC الفعلي (Binder/SharedMemory لا يُحاكيان بشكل صحيح على JVM)؛ التقدير بعد التصميم المفصَّل ٤-٧ أيام عمل، لا ٢-٣ كما قُدِّر أوّلياً |
| هجمات الذاكرة الباردة (Cold Boot) | منخفض | مفاتيح داخل Keystore/TEE-StrongBox عند توفّره على الجهاز |

## جهة الاتصال الأمنية

**لم تُحدَّد بعد.** لا تُستخدَم `security@securemessenger.app` — هذا عنوان لم يُتحقَّق من وجوده أو مراقبته فعلياً؛ أزيلَ من هذا الملف بدل الإيحاء بقناة تواصل تعمل.

---

## أوامر التحقّق الفعلية (مُختبَرة هذه الجلسة)

```bash
# اختبارات الجهاز الحقيقي (حاجز الإصدار) — يتطلّب جهازاً/محاكياً متّصلاً عبر adb
./gradlew connectedDebugAndroidTest

# تصريف + اختبارات الوحدة (لا يحتاج جهازاً، لا يتحقّق من libsodium/Keystore الحقيقيين)
./gradlew assembleDebug compileDebugUnitTestKotlin

# فحص الصلاحيات المُدمَجة فعلياً بعد التثبيت (لا الملف المصدري وحده)
adb shell dumpsys package com.securemessenger.app | grep -A 20 "requested permissions:"
```

**لا تستخدم** `./gradlew spotbugsMain` أو `python3 security_test.py`/`memory_test.py` — كانت في نسخة سابقة من هذا الملف، ولا وجود لأي منها في هذا المشروع.
